---
title: 已知问题
description: Captain Who 当前已确认的产品限制与尚未完成的发布能力。
status: current
audience: user
owner: product
last_verified: 2026-09-14
---

# 已知问题

本页列出当前实现中会影响用户预期的主要限制。具体下载、支持平台和当前版本信息以[版本与发布](../releases/README.md)为准。

## 发布与平台

- 不要从仓库中的构建入口推断某个平台已经正式支持；请只使用[版本与发布](../releases/README.md)列出的官方下载和平台信息。
- 正式签名 macOS 发行版只有在完整初始化后能够访问官方更新源、且该源提供新版本时才会显示更新入口。用户选择下载后，下载、完整性校验和原生准备完成，应用会退出并重新启动以安装。当前不提供自动降级或自动回滚。升级失败时不要假定把版本替换回旧包即可安全恢复数据。
- Windows 和 Linux 的构建入口不等同于公开发行支持；不要把其他平台的安装目录、组件缓存或内部文件复制到当前安装中。

## 数据与凭据

- 模型、搜索和图片生成密钥不进入当前 SQLite，但系统凭据不会随 SQLite 备份恢复；跨设备、跨账户或凭据后端不可用时需要替换或清除。旧版本 SQLite 备份仍可能含明文凭据。
- 账户登录和软件许可校验只处理账号准入，不同步桌面任务数据；无法完成校验时，新的 Agent 回合可能无法启动。
- 没有面向用户的一键数据导出、诊断包或完整备份/恢复向导。
- 移除项目会永久删除应用内的项目对话与附件记录；项目目录文件不会被删除。
- 当前没有统一的 Automation 历史/Event 与通用 Notification event/batch 保留或清理策略。

## 模型与上下文

- 兼容 Provider 是否真正支持 Tool、流式响应和 Usage，需要逐端点验证。
- 模型上下文窗口、图片能力和价格主要由用户配置，错误值可能产生失败或不准确估算。
- Provider 专属续接状态不能保证跨 Provider 无损迁移。
- 极长任务仍可能达到上下文、Tool 迭代或压缩边界。

## Skill 与 MCP

- Skill 只在当前 Run 激活；没有跨对话常驻激活。
- Skill Script 依赖本机 Python 3，且没有操作系统级脚本沙箱；Installed/Workspace 脚本每次运行都需要批准，符合严格条件的应用内置脚本可自动批准。
- 外部 MCP Server 仅支持本机 stdio；HTTP、OAuth、环境变量/SecretRef 和 MCP Resources/Prompts 等尚未开放。
- Windows 上外部 MCP Server 的完整进程树隔离尚不能保证。
- MCP 调用在超时或断开后可能是结果未知，不能安全自动重试。

## 浏览器与制品

- 受管网页默认不能使用摄像头、麦克风、定位和网页通知。
- 浏览器自动化不连接系统浏览器，只操作应用管理的浏览器环境。
- 浏览器下载会持久保存，但当前没有自动清理或统一保留期限；清除下载历史不删除磁盘文件。
- 截图等非下载 Browser Artifact 仍是 Run 生命周期资源；要长期保留必须显式导出。
- 扫描 PDF 没有文本层时可能无法读取；旧版 `.ppt`、`.xls` 不支持，`.doc` 读取仅限 macOS。

## Scheduled Automation

- 应用退出后不会由操作系统后台执行；错过的多个时间点会合并为一次恢复 Run。
- 不支持 cron、一次性计划、结束日期或独立时区选择器。
- 暂停任务不会停止当前 Run；界面没有独立的活动 Run 取消按钮。
- Blocked 状态不会因外部资源恢复自动解除，需要修复后重新保存。
- 没有 Renderer Toast 作为原生通知后备；通知可能延迟、缺失，或在极小崩溃窗口重复一次。
- 通知点击发生在界面尚未准备好时最多排队 32 个；超出时最早请求会被丢弃。

遇到未列出的可重复问题时，可按[诊断信息与日志](diagnostics-and-logs.md)在本地保留脱敏的最小复现材料。
